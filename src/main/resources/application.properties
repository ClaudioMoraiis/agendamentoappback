spring.application.name=demo

# Servidor
server.port=8081
server.address=0.0.0.0

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/agendamentoAPP?useSSL=false&serverTimezone=America/Sao_Paulo
spring.datasource.username=root
spring.datasource.password=

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Mail
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:eoquebn@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:ivzt ikqd oyzt lzkr}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_STARTTLS:true}
spring.mail.from=${SPRING_MAIL_FROM:eoquebn@gmail.com}

# Frontend / reset de senha
app.frontend.reset-url=https://agendamento.claudiomoraisdev.com.br
app.password-reset.expose-token=true
app.mail.expose-error=true

# Origins extras (separadas por vírgula), além das fixas no CorsConfig
app.allowed.origins=

# JWT
api.security.token.secret=AgendamentoDev_2026_Claudio_9fK7mQ2xL8pR4vN6
